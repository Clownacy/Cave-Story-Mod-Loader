This is a small patcher for installing the mod loader DLL loader in the EXE, for those that don't know how to use a hex editor. It also performs some minor integrity checks, to detect potential incompatibilities.

This is now deprecated, and has been replaced with the Bootstrapper.